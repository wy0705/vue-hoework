<template>
  <div>
    <h2>
      数据是用实值(例如浮点)来标记的，而不是标签。容易理解的例子是时间序列数据
      如股票价格随时间的变化，被建模的决策是对新的未预测数据的预测值。
    </h2>
    <p>
      数据没有标签，但可以根据数据中的相似性和其他自然结构的衡量标准分为几组。上面列表中的一个例子是用没有名字的面孔组织图片，人类用户必须为组指定名称，比如Mac上的iPhoto。
    </p>
  </div>
</template>
